import { and, desc, eq, gte, inArray, like, lte, max, or, sql } from "drizzle-orm";
import { TRPCError } from "@trpc/server";
import { z } from "zod";
import { auditLogs, attendance, customerBalanceDeliveries, customRoles, customers, inventoryItems, invoiceDeliveries, invoicePayments, invoices, measurementProfiles, pendingAccessRequests, performanceRecords, posPayments, salaryPayouts, saleItems, sales, services, shopSettings, staffAccessInvites, staffDocuments, staffProfiles, stockMovements, tailoringOrders, userBusinessRoles, userCustomRoles, users, type BusinessRole } from "../drizzle/schema";
import { getDb } from "./db";
import { effectiveInventoryQuantity, rollDerivedQuantity } from "./inventoryQuantity";
export { rollDerivedQuantity } from "./inventoryQuantity";
import { storageGetSignedUrl, storagePut } from "./storage";
import { protectedProcedure, router } from "./_core/trpc";

const adminRoles: BusinessRole[] = ["admin"]; const salesRoles: BusinessRole[] = ["admin", "sales"];const catalogRoles: BusinessRole[] = ["admin", "sales", "inventory"]; const inventoryRoles: BusinessRole[] = ["admin", "inventory"]; const payrollRoles: BusinessRole[] = ["admin", "payroll"]; const tailoringRoles: BusinessRole[] = ["admin", "sales", "tailor"]; const staffDirectoryRoles: BusinessRole[] = ["admin", "payroll", "sales", "tailor"];
const staffDocumentMimeTypes = new Set(["application/pdf", "image/png", "image/jpeg", "image/webp", "application/msword", "application/vnd.openxmlformats-officedocument.wordprocessingml.document"]);
const staffDocumentExtensions = new Set([".pdf", ".png", ".jpg", ".jpeg", ".webp", ".doc", ".docx"]);
export const customPermissionValues = ["dashboard", "customers", "sales", "inventory", "payroll", "production"] as const;
export type CustomPermission = (typeof customPermissionValues)[number];
const legacyPermissions: Record<BusinessRole, CustomPermission[]> = { admin: ["dashboard", "customers", "sales", "inventory", "payroll", "production"], sales: ["dashboard", "customers", "sales"], tailor: ["customers", "production"], inventory: ["inventory"], payroll: ["payroll"] };
export const customRoleCanAccess = (permissions: string[], allowed: BusinessRole[]) => allowed.filter(role => role !== "admin").some(role => legacyPermissions[role].some(permission => permissions.includes(permission)));
const three = (value: number) => value.toFixed(3);
const getMonthBounds = (payPeriod: string) => { const start = new Date(`${payPeriod}-01T00:00:00.000Z`); const end = new Date(start); end.setUTCMonth(end.getUTCMonth() + 1); return { start, end: new Date(end.getTime() - 1) }; };
const absenceDeductionFor = (baseSalary: number, status: string, payPeriod: string) => { const [year, month] = payPeriod.split("-").map(Number); const daysInMonth = new Date(Date.UTC(year, month, 0)).getUTCDate(); const daily = baseSalary / daysInMonth; return status === "absent" ? daily : status === "half_day" ? daily / 2 : 0; };
const id = (result: unknown) => { const row = Array.isArray(result) ? result[0] : result; return Number((row as { id?: number })?.id || 0); };
const dashboardRange = z.enum(["today", "7d", "30d", "90d", "all", "custom"]);
export const getDashboardRangeStart = (range: z.infer<typeof dashboardRange>, now = new Date()) => { const start = new Date(now); if (range === "all") return null; if (range === "today") { start.setHours(0, 0, 0, 0); return start; } if (range === "7d") start.setDate(start.getDate() - 6); if (range === "30d") start.setDate(start.getDate() - 29); if (range === "90d") start.setDate(start.getDate() - 89); start.setHours(0, 0, 0, 0); return start; };
export const getCustomDashboardRange = (startDate: string, endDate: string) => ({ start: new Date(`${startDate}T00:00:00.000Z`), end: new Date(`${endDate}T23:59:59.999Z`) });
const dashboardInput = z.object({ range: dashboardRange, startDate: z.string().regex(/^\d{4}-\d{2}-\d{2}$/).optional(), endDate: z.string().regex(/^\d{4}-\d{2}-\d{2}$/).optional() }).superRefine((value, ctx) => { if (value.range === "custom" && (!value.startDate || !value.endDate)) ctx.addIssue({ code: "custom", message: "Choose both a start and end date for a custom period." }); if (value.range === "custom" && value.startDate && value.endDate && value.startDate > value.endDate) ctx.addIssue({ code: "custom", message: "The custom period end date must be on or after its start date." }); });
const salesSource = z.enum(["counter", "manual", "tailoring"]);
const salesHistoryInput = z.object({ search: z.string().max(160).optional(), source: salesSource.optional(), paymentStatus: z.enum(["paid", "partial", "unpaid"]).optional(), startDate: z.string().regex(/^\d{4}-\d{2}-\d{2}$/).optional(), endDate: z.string().regex(/^\d{4}-\d{2}-\d{2}$/).optional() }).superRefine((value, ctx) => { if (value.startDate && value.endDate && value.startDate > value.endDate) ctx.addIssue({ code: "custom", message: "The end date must be on or after the start date." }); });
const invoiceListInput = z.object({ search: z.string().max(160).optional(), status: z.enum(["paid", "partial", "unpaid"]).optional(), source: salesSource.optional(), paymentMethod: z.enum(["cash", "benefitpay", "bank_transfer", "credit_card"]).optional(), startDate: z.string().regex(/^\d{4}-\d{2}-\d{2}$/).optional(), endDate: z.string().regex(/^\d{4}-\d{2}-\d{2}$/).optional() }).superRefine((value, ctx) => { if (value.startDate && value.endDate && value.startDate > value.endDate) ctx.addIssue({ code: "custom", message: "The end date must be on or after the start date." }); });
const invoicePaymentInput = z.object({ invoiceId: z.number().int().positive(), amount: z.number().positive().max(1000000), paymentMethod: z.enum(["cash", "benefitpay", "bank_transfer", "credit_card"]), reference: z.string().trim().max(160).optional() });
const invoiceDeleteInput = z.object({ invoiceId: z.number().int().positive() });
const manualSaleInput = z.object({ customerId: z.number().int().positive().optional(), customerName: z.string().trim().min(2).max(160), customerPhone: z.string().trim().max(50), description: z.string().trim().min(2).max(160), quantity: z.number().positive().max(999), unitPrice: z.number().positive().max(1000000), discount: z.number().min(0).max(1000000), paymentMethod: z.enum(["cash", "benefitpay", "bank_transfer", "credit_card"]), paymentStatus: z.enum(["paid", "partial", "unpaid"]), notes: z.string().max(2000) }).superRefine((value, ctx) => { if (value.discount > value.quantity * value.unitPrice) ctx.addIssue({ code: "custom", path: ["discount"], message: "Discount cannot exceed the sale subtotal." }); });
const monthInput = z.object({ month: z.string().regex(/^\d{4}-(0[1-9]|1[0-2])$/, "Choose a valid report month.") });
export const getMonthWindow = (month: string) => { const [year, monthIndex] = month.split("-").map(Number); return { start: new Date(Date.UTC(year, monthIndex - 1, 1)), end: new Date(Date.UTC(year, monthIndex, 0, 23, 59, 59, 999)) }; };
async function dbOrThrow() { const db = await getDb(); if (!db) throw new TRPCError({ code: "INTERNAL_SERVER_ERROR", message: "Database is unavailable." }); return db; }
async function access(userId: number, frameworkRole: "user" | "admin", allowed: BusinessRole[]) { const db = await dbOrThrow(); let record = (await db.select().from(userBusinessRoles).where(eq(userBusinessRoles.userId, userId)).limit(1))[0]; if (!record) { if (frameworkRole !== "admin") { const request = (await db.select().from(pendingAccessRequests).where(eq(pendingAccessRequests.userId, userId)).limit(1))[0]; const message = request?.status === "rejected" ? "Your ERP access request was not approved. Contact the shop owner." : "Your ERP access request is awaiting owner approval."; throw new TRPCError({ code: "FORBIDDEN", message }); } await db.insert(userBusinessRoles).values({ userId, role: "admin", isActive: true }); record = (await db.select().from(userBusinessRoles).where(eq(userBusinessRoles.userId, userId)).limit(1))[0]; } if (!record?.isActive) throw new TRPCError({ code: "FORBIDDEN", message: "Your ERP access is inactive." }); if (record.role === "admin") return record.role; const assignment = (await db.select().from(userCustomRoles).where(eq(userCustomRoles.userId, userId)).limit(1))[0]; if (assignment) { const role = (await db.select().from(customRoles).where(eq(customRoles.id, assignment.customRoleId)).limit(1))[0]; const permissions = Array.isArray(role?.permissionsJson) ? role.permissionsJson.filter((value): value is string => typeof value === "string") : []; if (!assignment.isActive || !role?.isActive || !customRoleCanAccess(permissions, allowed)) throw new TRPCError({ code: "FORBIDDEN", message: "Your owner-assigned role is not permitted to perform this action." }); return `custom:${role.name}`; } if (allowed.includes(record.role)) return record.role; throw new TRPCError({ code: "FORBIDDEN", message: "Your assigned role is not permitted to perform this action." }); }
async function audit(userId: number, action: string, entityType: string, entityId?: number, details?: unknown) { const db = await dbOrThrow(); await db.insert(auditLogs).values({ actorId: userId, action, entityType, entityId, detailsJson: details ? JSON.stringify(details) : null }); }
const customerInput = z.object({ name: z.string().min(2).max(160), phone: z.string().min(4).max(50), email: z.string().email().or(z.literal("")), address: z.string().max(1000), notes: z.string().max(3000), preferredContact: z.string().max(40) });
export const tailoringOrderStatuses = ["draft", "confirmed", "cutting", "stitching", "fitting", "ready", "handed_over", "cancelled"] as const;
const tailoringStatus = z.enum(tailoringOrderStatuses);
const tailoringListInput = z.object({ search: z.string().max(160).optional() });
export const tailoringRelationError = (customerId: number, measurementCustomerId: number | null | undefined, tailorActive: boolean | null | undefined) => { if (measurementCustomerId !== customerId) return "measurement" as const; if (tailorActive === false) return "tailor" as const; return null; };
const tailoringOrderInput = z.object({ customerId: z.number().int().positive(), measurementProfileId: z.number().int().positive().optional(), assignedTailorId: z.number().int().positive().optional(), garmentType: z.string().min(2).max(80), quantity: z.number().int().min(1).max(20), dueDate: z.string().optional(), price: z.number().min(0), customerSuppliedFabric: z.boolean().default(false), fabricNotes: z.string().max(2000).optional(), notes: z.string().max(3000), productionNotes: z.string().max(3000) });

export const erpRouter = router({
  shop: router({ get: protectedProcedure.query(async ({ ctx }) => { await access(ctx.user.id, ctx.user.role, ["admin", "sales", "inventory", "tailor", "payroll"]); return (await dbOrThrow()).select().from(shopSettings).limit(1).then(rows => rows[0] || null); }), save: protectedProcedure.input(z.object({ shopName: z.string().min(2), arabicShopName: z.string(), crNumber: z.string(), currency: z.string(), phone: z.string(), email: z.string(), address: z.string(), invoicePrefix: z.string().min(1).max(16), invoiceTerms: z.string().max(4000).optional(), vatEnabled: z.boolean().default(false), vatRate: z.number().min(0).max(100).default(10), vatNumber: z.string().max(80).optional() })).mutation(async ({ ctx, input }) => { await access(ctx.user.id, ctx.user.role, adminRoles); const db = await dbOrThrow(); const values = { ...input, vatRate: input.vatRate.toFixed(3), vatNumber: input.vatNumber || null, updatedBy: ctx.user.id }; const existing = (await db.select().from(shopSettings).limit(1))[0]; if (existing) await db.update(shopSettings).set(values).where(eq(shopSettings.id, existing.id)); else await db.insert(shopSettings).values(values); await audit(ctx.user.id, "SHOP_SETTINGS_SAVED", "shopSettings", existing?.id); return { success: true }; }) }),
  dashboard: protectedProcedure.input(dashboardInput.optional()).query(async ({ ctx, input }) => { await access(ctx.user.id, ctx.user.role, ["admin", "sales", "inventory", "tailor", "payroll"]); const db = await dbOrThrow(); const range = input?.range || "30d"; const customRange = range === "custom" && input?.startDate && input?.endDate ? getCustomDashboardRange(input.startDate, input.endDate) : null; const rangeStart = customRange?.start || getDashboardRangeStart(range); const salesWhere = customRange ? and(gte(sales.createdAt, customRange.start), lte(sales.createdAt, customRange.end)) : rangeStart ? gte(sales.createdAt, rangeStart) : undefined; const today = getDashboardRangeStart("today")!; const weekStart = getDashboardRangeStart("7d")!; const monthStart = getDashboardRangeStart("30d")!; const [customerCount] = await db.select({ count: sql<number>`count(*)` }).from(customers); const [inventoryCount] = await db.select({ count: sql<number>`count(*)` }).from(inventoryItems).where(eq(inventoryItems.isActive, true)); const [rangeMetrics] = await db.select({ total: sql<string>`coalesce(sum(${sales.total}), 0)`, count: sql<number>`count(*)` }).from(sales).where(salesWhere); const [todayMetrics] = await db.select({ total: sql<string>`coalesce(sum(${sales.total}), 0)` }).from(sales).where(gte(sales.createdAt, today)); const [weekMetrics] = await db.select({ total: sql<string>`coalesce(sum(${sales.total}), 0)` }).from(sales).where(gte(sales.createdAt, weekStart)); const [monthMetrics] = await db.select({ total: sql<string>`coalesce(sum(${sales.total}), 0)` }).from(sales).where(gte(sales.createdAt, monthStart)); const low = await db.select().from(inventoryItems).where(sql`${inventoryItems.quantity} <= ${inventoryItems.minThreshold}`).orderBy(inventoryItems.quantity).limit(8); const filteredSales = await db.select().from(sales).where(salesWhere).orderBy(desc(sales.createdAt)).limit(250); const revenueByDayMap = new Map<string, number>(); for (const sale of filteredSales) { const day = new Date(sale.createdAt).toISOString().slice(0, 10); revenueByDayMap.set(day, (revenueByDayMap.get(day) || 0) + Number(sale.total)); } const revenueByDay = Array.from(revenueByDayMap.entries()).sort(([left], [right]) => left.localeCompare(right)).map(([day, revenue]) => ({ day, revenue })); const topServices = await db.select({ name: saleItems.nameSnapshot, quantity: sql<string>`coalesce(sum(${saleItems.quantity}), 0)`, revenue: sql<string>`coalesce(sum(${saleItems.lineTotal}), 0)` }).from(saleItems).innerJoin(sales, eq(saleItems.saleId, sales.id)).where(salesWhere).groupBy(saleItems.nameSnapshot).orderBy(desc(sql`sum(${saleItems.lineTotal})`)).limit(5); const [activeStaff] = await db.select({ count: sql<number>`count(*)` }).from(staffProfiles).where(eq(staffProfiles.isActive, true)); const attendanceToday = await db.select({ status: attendance.status, count: sql<number>`count(*)` }).from(attendance).where(eq(attendance.workDate, today)).groupBy(attendance.status); const attendanceSummary = attendanceToday.reduce((summary, item) => ({ ...summary, [item.status]: Number(item.count || 0) }), { present: 0, absent: 0, leave: 0, half_day: 0 } as Record<string, number>); const total = Number(rangeMetrics?.total || 0); const saleCount = Number(rangeMetrics?.count || 0); return { range, customerCount: Number(customerCount?.count || 0), inventoryCount: Number(inventoryCount?.count || 0), totalSales: total, saleCount, averageOrderValue: saleCount ? total / saleCount : 0, todayRevenue: Number(todayMetrics?.total || 0), weekRevenue: Number(weekMetrics?.total || 0), monthRevenue: Number(monthMetrics?.total || 0), lowStock: low, recentSales: filteredSales.slice(0, 8), revenueByDay, topServices: topServices.map(item => ({ ...item, quantity: Number(item.quantity), revenue: Number(item.revenue) })), staff: { activeCount: Number(activeStaff?.count || 0), attendance: attendanceSummary } }; }),
  customers: router({ list: protectedProcedure.input(z.object({ search: z.string().optional() }).optional()).query(async ({ ctx, input }) => { await access(ctx.user.id, ctx.user.role, ["admin", "sales", "tailor"]); const db = await dbOrThrow(); const search = input?.search?.trim(); const where = search ? and(eq(customers.isActive, true), or(like(customers.name, `%${search}%`), like(customers.phone, `%${search}%`))) : eq(customers.isActive, true); return db.select().from(customers).where(where).orderBy(desc(customers.createdAt)).limit(20); }), create: protectedProcedure.input(customerInput).mutation(async ({ ctx, input }) => { await access(ctx.user.id, ctx.user.role, salesRoles); const db = await dbOrThrow(); const result = await db.insert(customers).values({ ...input, email: input.email || null, address: input.address || null, notes: input.notes || null }).returning({ id: customers.id }); const customerId = id(result); await audit(ctx.user.id, "CUSTOMER_CREATED", "customer", customerId); return { id: customerId }; }), update: protectedProcedure.input(customerInput.extend({ id: z.number().int().positive() })).mutation(async ({ ctx, input }) => { await access(ctx.user.id, ctx.user.role, salesRoles); const db = await dbOrThrow(); await db.update(customers).set({ ...input, email: input.email || null, address: input.address || null, notes: input.notes || null }).where(eq(customers.id, input.id)); await audit(ctx.user.id, "CUSTOMER_UPDATED", "customer", input.id); return { success: true }; }), remove: protectedProcedure.input(z.object({ id: z.number().int().positive() })).mutation(async ({ ctx, input }) => { await access(ctx.user.id, ctx.user.role, salesRoles); const db = await dbOrThrow(); const customer = (await db.select({ id: customers.id, name: customers.name }).from(customers).where(eq(customers.id, input.id)).limit(1))[0]; if (!customer) throw new TRPCError({ code: "NOT_FOUND", message: "Customer not found." }); await db.update(customers).set({ isActive: false, updatedAt: new Date() }).where(eq(customers.id, input.id)); await audit(ctx.user.id, "CUSTOMER_ARCHIVED", "customer", input.id, { name: customer.name }); return { success: true, id: input.id, name: customer.name }; }), balance: protectedProcedure.input(z.object({ customerId: z.number().int().positive() })).query(async ({ ctx, input }) => { await access(ctx.user.id, ctx.user.role, salesRoles); const db = await dbOrThrow(); const customer = (await db.select().from(customers).where(eq(customers.id, input.customerId)).limit(1))[0]; if (!customer) throw new TRPCError({ code: "NOT_FOUND", message: "Customer not found." }); const rows = await db.select().from(sales).where(eq(sales.customerId, input.customerId)).orderBy(desc(sales.createdAt)).limit(500); const balance = rows.reduce((sum, sale) => sum + Math.max(0, Number(sale.total) - Number(sale.paidAmount)), 0); return { customer, balance, openInvoices: rows.filter(sale => Number(sale.total) - Number(sale.paidAmount) > 0.001).map(sale => ({ saleNumber: sale.saleNumber, total: Number(sale.total), paidAmount: Number(sale.paidAmount), outstanding: Number(sale.total) - Number(sale.paidAmount), createdAt: sale.createdAt })) }; }), sendBalance: protectedProcedure.input(z.object({ customerId: z.number().int().positive(), channel: z.enum(["email", "whatsapp", "share"]), recipient: z.string().trim().max(320).optional(), message: z.string().max(4000).optional() })).mutation(async ({ ctx, input }) => { await access(ctx.user.id, ctx.user.role, salesRoles); const db = await dbOrThrow(); const customer = (await db.select().from(customers).where(eq(customers.id, input.customerId)).limit(1))[0]; if (!customer) throw new TRPCError({ code: "NOT_FOUND", message: "Customer not found." }); const recipient = input.recipient || (input.channel === "email" ? customer.email || undefined : customer.phone || undefined); if (!recipient) throw new TRPCError({ code: "BAD_REQUEST", message: `Add a ${input.channel === "email" ? "customer email" : "customer phone number"} before sending the balance.` }); const balance = (await db.select().from(sales).where(eq(sales.customerId, input.customerId)).limit(500)).reduce((sum, sale) => sum + Math.max(0, Number(sale.total) - Number(sale.paidAmount)), 0); const message = input.message || `Dear ${customer.name}, your current outstanding balance is ${balance.toFixed(3)} BHD.`; const result = await db.insert(customerBalanceDeliveries).values({ customerId: customer.id, channel: input.channel, recipient, message, status: "prepared", createdBy: ctx.user.id }).returning({ id: customerBalanceDeliveries.id }); await audit(ctx.user.id, "CUSTOMER_BALANCE_DELIVERY_PREPARED", "customer", customer.id, { channel: input.channel, recipient, balance }); return { id: Number(result[0]?.id || 0), channel: input.channel, recipient, balance, message }; }), measurements: protectedProcedure.input(z.object({ customerId: z.number().int().positive() })).query(async ({ ctx, input }) => { await access(ctx.user.id, ctx.user.role, ["admin", "sales", "tailor"]); return (await dbOrThrow()).select().from(measurementProfiles).where(eq(measurementProfiles.customerId, input.customerId)).orderBy(desc(measurementProfiles.version)); }), addMeasurement: protectedProcedure.input(z.object({ customerId: z.number().int(), measurements: z.record(z.string(), z.string()), fitPreference: z.string(), collarStyle: z.string(), pocketStyle: z.string(), notes: z.string(), effectiveDate: z.string() })).mutation(async ({ ctx, input }) => { await access(ctx.user.id, ctx.user.role, ["admin", "tailor", "sales"]); const db = await dbOrThrow(); const latest = (await db.select({ version: max(measurementProfiles.version) }).from(measurementProfiles).where(eq(measurementProfiles.customerId, input.customerId)))[0]; const result = await db.insert(measurementProfiles).values({ ...input, measurementsJson: input.measurements, version: Number(latest?.version || 0) + 1, effectiveDate: new Date(input.effectiveDate), createdBy: ctx.user.id }).returning({ id: measurementProfiles.id }); const profileId = id(result); await audit(ctx.user.id, "MEASUREMENT_VERSION_CREATED", "measurementProfile", profileId); return { id: profileId }; }) }),
    inventory: router({ list: protectedProcedure.query(async ({ ctx }) => { await access(ctx.user.id, ctx.user.role, ["admin", "sales", "inventory"]); const db = await dbOrThrow(); const items = await db.select().from(inventoryItems).where(eq(inventoryItems.isActive, true)).orderBy(inventoryItems.name); const ids = items.map(item => item.id); const movementRows = ids.length ? await db.select({ inventoryItemId: stockMovements.inventoryItemId }).from(stockMovements).where(inArray(stockMovements.inventoryItemId, ids)).groupBy(stockMovements.inventoryItemId) : []; const movementIds = new Set(movementRows.map(row => row.inventoryItemId)); return items.map(item => ({ ...item, quantity: three(effectiveInventoryQuantity({ inventoryType: item.inventoryType, quantity: item.quantity, rollCount: item.rollCount, metersPerRoll: item.metersPerRoll, hasMovement: movementIds.has(item.id) })) })); }),
 create: protectedProcedure.input(z.object({ code: z.string().min(1), name: z.string().min(2), inventoryType: z.enum(["material", "item"]).default("material"), category: z.enum(["fabric", "lining", "buttons", "thread", "accessory", "other"]), color: z.string(), size: z.string().optional(), widthInches: z.number().optional(), unit: z.string().min(1), minThreshold: z.number().min(0), costPerUnit: z.number().min(0), salePrice: z.number().min(0).default(0), openingQuantity: z.number().min(0), rollCount: z.number().int().min(0).default(0), metersPerRoll: z.number().positive().optional() })).mutation(async ({ ctx, input }) => { await access(ctx.user.id, ctx.user.role, inventoryRoles); const db = await dbOrThrow(); const openingQuantity = rollDerivedQuantity(input.inventoryType, input.rollCount, input.metersPerRoll) ?? input.openingQuantity; const result = await db.insert(inventoryItems).values({ ...input, color: input.color || null, size: input.size || null, widthInches: input.widthInches?.toString(), quantity: three(openingQuantity), rollCount: input.rollCount, metersPerRoll: input.metersPerRoll?.toString() || null, minThreshold: three(input.minThreshold), costPerUnit: three(input.costPerUnit), salePrice: three(input.salePrice) }).returning({ id: inventoryItems.id }); const itemId = id(result); if (openingQuantity) await db.insert(stockMovements).values({ inventoryItemId: itemId, movementType: "opening", quantityChange: three(openingQuantity), quantityBefore: "0.000", quantityAfter: three(openingQuantity), createdBy: ctx.user.id, notes: "Opening balance" }); await audit(ctx.user.id, "INVENTORY_CREATED", "inventoryItem", itemId); return { id: itemId }; }), update: protectedProcedure.input(z.object({ id: z.number().int().positive(), code: z.string().min(1), name: z.string().min(2), inventoryType: z.enum(["material", "item"]).default("material"), category: z.enum(["fabric", "lining", "buttons", "thread", "accessory", "other"]), color: z.string(), size: z.string().optional(), widthInches: z.number().optional(), unit: z.string().min(1), minThreshold: z.number().min(0), costPerUnit: z.number().min(0), salePrice: z.number().min(0).default(0), rollCount: z.number().int().min(0), metersPerRoll: z.number().positive().optional() })).mutation(async ({ ctx, input }) => { await access(ctx.user.id, ctx.user.role, inventoryRoles); const db = await dbOrThrow(); const item = (await db.select().from(inventoryItems).where(eq(inventoryItems.id, input.id)).limit(1))[0]; if (!item) throw new TRPCError({ code: "NOT_FOUND", message: "Inventory item not found." }); const seedQuantity = Number(item.quantity) <= 0 ? rollDerivedQuantity(input.inventoryType, input.rollCount, input.metersPerRoll) : null; const quantityBefore = Number(item.quantity); const quantityAfter = seedQuantity ?? quantityBefore; await db.transaction(async tx => { await tx.update(inventoryItems).set({ code: input.code, name: input.name, inventoryType: input.inventoryType, category: input.category, color: input.color || null, size: input.size || null, widthInches: input.widthInches?.toString(), unit: input.unit, quantity: three(quantityAfter), minThreshold: three(input.minThreshold), costPerUnit: three(input.costPerUnit), salePrice: three(input.salePrice), rollCount: input.rollCount, metersPerRoll: input.metersPerRoll?.toString() || null }).where(eq(inventoryItems.id, input.id)); if (seedQuantity !== null && seedQuantity > 0) await tx.insert(stockMovements).values({ inventoryItemId: input.id, movementType: "opening", quantityChange: three(seedQuantity), quantityBefore: three(quantityBefore), quantityAfter: three(seedQuantity), createdBy: ctx.user.id, notes: "Opening balance derived from roll count × meters per roll" }); }); await audit(ctx.user.id, "INVENTORY_UPDATED", "inventoryItem", input.id, { ...input, quantity: quantityAfter, quantityDerivedFromRolls: seedQuantity !== null }); return { success: true }; }), adjust: protectedProcedure.input(z.object({ inventoryItemId: z.number().int(), quantityChange: z.number(), rollCountChange: z.number().int().default(0), notes: z.string().max(1000) })).mutation(async ({ ctx, input }) => { await access(ctx.user.id, ctx.user.role, inventoryRoles); const db = await dbOrThrow(); const item = (await db.select().from(inventoryItems).where(eq(inventoryItems.id, input.inventoryItemId)).limit(1))[0]; if (!item) throw new TRPCError({ code: "NOT_FOUND", message: "Inventory item not found." }); const before = Number(item.quantity); const rollQuantityChange = item.inventoryType === "material" && input.quantityChange === 0 && input.rollCountChange !== 0 && Number(item.metersPerRoll) > 0 ? input.rollCountChange * Number(item.metersPerRoll) : 0; const effectiveQuantityChange = input.quantityChange !== 0 ? input.quantityChange : rollQuantityChange; const after = before + effectiveQuantityChange; const rollCountAfter = Number(item.rollCount || 0) + input.rollCountChange; if (effectiveQuantityChange === 0 && input.rollCountChange === 0) throw new TRPCError({ code: "BAD_REQUEST", message: "Enter a meter or roll adjustment." }); if (after < 0 || rollCountAfter < 0) throw new TRPCError({ code: "BAD_REQUEST", message: "Stock cannot drop below zero." }); await db.update(inventoryItems).set({ quantity: three(after), rollCount: rollCountAfter }).where(eq(inventoryItems.id, item.id)); await db.insert(stockMovements).values({ inventoryItemId: item.id, movementType: "adjustment", quantityChange: three(effectiveQuantityChange), quantityBefore: three(before), quantityAfter: three(after), createdBy: ctx.user.id, notes: input.notes || null }); await audit(ctx.user.id, "STOCK_ADJUSTED", "inventoryItem", item.id, input); return { quantity: after }; }), remove: protectedProcedure.input(z.object({ id: z.number().int().positive() })).mutation(async ({ ctx, input }) => { await access(ctx.user.id, ctx.user.role, inventoryRoles); const db = await dbOrThrow(); const item = (await db.select({ id: inventoryItems.id, name: inventoryItems.name }).from(inventoryItems).where(eq(inventoryItems.id, input.id)).limit(1))[0]; if (!item) throw new TRPCError({ code: "NOT_FOUND", message: "Inventory item not found." }); await db.update(inventoryItems).set({ isActive: false, updatedAt: new Date() }).where(eq(inventoryItems.id, input.id)); await audit(ctx.user.id, "INVENTORY_ARCHIVED", "inventoryItem", input.id, { name: item.name }); return { success: true, id: input.id, name: item.name }; }) }),
  services: router({ list: protectedProcedure.query(async ({ ctx }) => { await access(ctx.user.id, ctx.user.role, catalogRoles); const records = await (await dbOrThrow()).select({ service: services, inventory: inventoryItems }).from(services).leftJoin(inventoryItems, eq(services.inventoryItemId, inventoryItems.id)).where(eq(services.isActive, true)).orderBy(services.name); return records.map(record => ({ ...record.service, inventory: record.inventory?.id ? { id: record.inventory.id, code: record.inventory.code, name: record.inventory.name, quantity: record.inventory.quantity, unit: record.inventory.unit, isActive: record.inventory.isActive } : null })); }), create: protectedProcedure.input(z.object({ sku: z.string().min(1), name: z.string().min(2), category: z.enum(["tailoring", "fabric", "alteration", "accessory", "other"]), description: z.string(), unitPrice: z.number().positive(), inventoryItemId: z.number().int().optional(), defaultFabricMeters: z.number().optional() })).mutation(async ({ ctx, input }) => { await access(ctx.user.id, ctx.user.role, inventoryRoles); const result = await (await dbOrThrow()).insert(services).values({ ...input, description: input.description || null, unitPrice: three(input.unitPrice), defaultFabricMeters: input.defaultFabricMeters?.toString() }).returning({ id: services.id }); const serviceId = id(result); await audit(ctx.user.id, "SERVICE_CREATED", "service", serviceId); return { id: serviceId }; }) }),
  sales: router({ list: protectedProcedure.query(async ({ ctx }) => { await access(ctx.user.id, ctx.user.role, salesRoles); return (await dbOrThrow()).select().from(sales).orderBy(desc(sales.createdAt)).limit(100); }), create: protectedProcedure.input(z.object({ customerId: z.number().int().optional(), customerName: z.string().min(1), customerPhone: z.string().optional(), discount: z.number().min(0), paymentMethod: z.enum(["cash", "benefitpay", "bank_transfer", "credit_card"]), paymentStatus: z.enum(["paid", "partial", "unpaid"]), items: z.array(z.object({ serviceId: z.number().int().optional(), inventoryItemId: z.number().int().optional(), name: z.string().min(1), quantity: z.number().positive(), unitPrice: z.number().nonnegative() })).min(1) })).mutation(async ({ ctx, input }) => { await access(ctx.user.id, ctx.user.role, salesRoles); const db = await dbOrThrow(); const shop = (await db.select().from(shopSettings).limit(1))[0]; const subtotal = input.items.reduce((sum, item) => sum + item.quantity * item.unitPrice, 0); const total = Math.max(0, subtotal - input.discount); const saleNumber = `SALE-${Date.now()}`; const invoicePrefix = shop?.invoicePrefix || "INV"; const saleId = await db.transaction(async tx => { const saleResult = await tx.insert(sales).values({ saleNumber, customerId: input.customerId, customerNameSnapshot: input.customerName, customerPhoneSnapshot: input.customerPhone || null, subtotal: three(subtotal), discount: three(input.discount), total: three(total), paymentMethod: input.paymentMethod, paymentStatus: input.paymentStatus, createdBy: ctx.user.id }).returning({ id: sales.id }); const createdId = id(saleResult); for (const item of input.items) { await tx.insert(saleItems).values({ saleId: createdId, serviceId: item.serviceId, inventoryItemId: item.inventoryItemId, nameSnapshot: item.name, quantity: three(item.quantity), unitPrice: three(item.unitPrice), lineTotal: three(item.quantity * item.unitPrice) }); if (item.inventoryItemId) { const stock = (await tx.select().from(inventoryItems).where(eq(inventoryItems.id, item.inventoryItemId)).limit(1))[0]; if (!stock) throw new TRPCError({ code: "BAD_REQUEST", message: "Linked inventory item was not found." }); const before = Number(stock.quantity); const after = before - item.quantity; if (after < 0) throw new TRPCError({ code: "BAD_REQUEST", message: `${stock.name} does not have enough stock.` }); await tx.update(inventoryItems).set({ quantity: three(after) }).where(eq(inventoryItems.id, stock.id)); await tx.insert(stockMovements).values({ inventoryItemId: stock.id, movementType: "sale", quantityChange: three(-item.quantity), quantityBefore: three(before), quantityAfter: three(after), referenceType: "sale", referenceId: createdId, createdBy: ctx.user.id, notes: saleNumber }); } } await tx.insert(invoices).values({ saleId: createdId, invoiceNumber: `${invoicePrefix}-${String(createdId).padStart(6, "0")}`, status: input.paymentStatus }); return createdId; }); await audit(ctx.user.id, "SALE_COMPLETED", "sale", saleId, { total }); return { id: saleId, total }; }) }),
  salesHistory: router({ list: protectedProcedure.input(salesHistoryInput.optional()).query(async ({ ctx, input }) => { await access(ctx.user.id, ctx.user.role, salesRoles); const db = await dbOrThrow(); const [saleRows, invoiceRows] = await Promise.all([db.select().from(sales).orderBy(desc(sales.createdAt)).limit(500), db.select().from(invoices)]); const invoiceBySale = new Map(invoiceRows.map(invoice => [invoice.saleId, invoice])); const search = input?.search?.trim().toLowerCase(); const start = input?.startDate ? new Date(`${input.startDate}T00:00:00.000Z`) : null; const end = input?.endDate ? new Date(`${input.endDate}T23:59:59.999Z`) : null; return saleRows.filter(sale => (!input?.source || sale.source === input.source) && (!input?.paymentStatus || sale.paymentStatus === input.paymentStatus) && (!start || sale.createdAt >= start) && (!end || sale.createdAt <= end) && (!search || [sale.saleNumber, sale.customerNameSnapshot, sale.customerPhoneSnapshot || ""].some(value => value.toLowerCase().includes(search)))).map(sale => ({ ...sale, invoice: invoiceBySale.get(sale.id) || null })); }), monthlyReport: protectedProcedure.input(monthInput).query(async ({ ctx, input }) => { await access(ctx.user.id, ctx.user.role, salesRoles); const db = await dbOrThrow(); const { start, end } = getMonthWindow(input.month); const [allSales, invoiceRows, allItems, shop] = await Promise.all([db.select().from(sales).orderBy(desc(sales.createdAt)).limit(1000), db.select().from(invoices), db.select().from(saleItems), db.select().from(shopSettings).limit(1).then(rows => rows[0] || null)]); const rows = allSales.filter(sale => sale.createdAt >= start && sale.createdAt <= end); const invoiceBySale = new Map(invoiceRows.map(invoice => [invoice.saleId, invoice])); const includedIds = new Set(rows.map(sale => sale.id)); const bySource = new Map<string, { count: number; total: number }>(); const byPayment = new Map<string, { count: number; total: number }>(); const topLines = new Map<string, { quantity: number; total: number }>(); for (const sale of rows) { const source = bySource.get(sale.source) || { count: 0, total: 0 }; source.count += 1; source.total += Number(sale.total); bySource.set(sale.source, source); const payment = byPayment.get(sale.paymentMethod) || { count: 0, total: 0 }; payment.count += 1; payment.total += Number(sale.total); byPayment.set(sale.paymentMethod, payment); } for (const line of allItems) if (includedIds.has(line.saleId)) { const current = topLines.get(line.nameSnapshot) || { quantity: 0, total: 0 }; current.quantity += Number(line.quantity); current.total += Number(line.lineTotal); topLines.set(line.nameSnapshot, current); } const total = rows.reduce((sum, sale) => sum + Number(sale.total), 0); return { month: input.month, shop, totals: { saleCount: rows.length, revenue: total, averageOrder: rows.length ? total / rows.length : 0, paidCount: rows.filter(sale => sale.paymentStatus === "paid").length, partialCount: rows.filter(sale => sale.paymentStatus === "partial").length, unpaidCount: rows.filter(sale => sale.paymentStatus === "unpaid").length }, bySource: Array.from(bySource, ([source, values]) => ({ source, ...values })), byPayment: Array.from(byPayment, ([paymentMethod, values]) => ({ paymentMethod, ...values })), topLines: Array.from(topLines, ([name, values]) => ({ name, ...values })).sort((left, right) => right.total - left.total).slice(0, 8), sales: rows.map(sale => ({ ...sale, invoice: invoiceBySale.get(sale.id) || null })) }; }) }),
  invoices: router({ list: protectedProcedure.input(invoiceListInput.optional()).query(async ({ ctx, input }) => { await access(ctx.user.id, ctx.user.role, salesRoles); const db = await dbOrThrow(); const search = input?.search?.trim().toLowerCase(); const start = input?.startDate ? new Date(`${input.startDate}T00:00:00.000Z`) : null; const end = input?.endDate ? new Date(`${input.endDate}T23:59:59.999Z`) : null; const [invoiceRows, saleRows] = await Promise.all([db.select().from(invoices).orderBy(desc(invoices.issuedAt)).limit(500), db.select().from(sales).orderBy(desc(sales.createdAt)).limit(500)]); const saleById = new Map(saleRows.map(sale => [sale.id, sale])); return invoiceRows.map(invoice => ({ ...invoice, sale: saleById.get(invoice.saleId) || null })).filter(row => (!input?.status || row.status === input.status) && (!input?.source || row.sale?.source === input.source) && (!input?.paymentMethod || row.sale?.paymentMethod === input.paymentMethod) && (!start || row.issuedAt >= start) && (!end || row.issuedAt <= end) && (!search || [row.invoiceNumber, row.sale?.saleNumber || "", row.sale?.customerNameSnapshot || "", row.sale?.customerPhoneSnapshot || ""].some(value => value.toLowerCase().includes(search)))); }), detail: protectedProcedure.input(z.object({ invoiceId: z.number().int().positive() })).query(async ({ ctx, input }) => { await access(ctx.user.id, ctx.user.role, salesRoles); const db = await dbOrThrow(); const invoice = (await db.select().from(invoices).where(eq(invoices.id, input.invoiceId)).limit(1))[0]; if (!invoice) throw new TRPCError({ code: "NOT_FOUND", message: "Invoice not found." }); const sale = (await db.select().from(sales).where(eq(sales.id, invoice.saleId)).limit(1))[0]; if (!sale) throw new TRPCError({ code: "NOT_FOUND", message: "Sale for this invoice was not found." }); const items = await db.select().from(saleItems).where(eq(saleItems.saleId, sale.id)); const shop = (await db.select().from(shopSettings).limit(1))[0] || null; const [deliveries, paymentRecords] = await Promise.all([db.select().from(invoiceDeliveries).where(eq(invoiceDeliveries.invoiceId, invoice.id)).orderBy(desc(invoiceDeliveries.createdAt)).limit(20), db.select().from(invoicePayments).where(eq(invoicePayments.invoiceId, invoice.id)).orderBy(invoicePayments.createdAt, invoicePayments.id).limit(200)]); return { invoice, sale, items, shop, deliveries, paymentRecords }; }), addPayment: protectedProcedure.input(invoicePaymentInput).mutation(async ({ ctx, input }) => { await access(ctx.user.id, ctx.user.role, salesRoles); const db = await dbOrThrow(); const result = await db.transaction(async tx => { const invoice = (await tx.select().from(invoices).where(eq(invoices.id, input.invoiceId)).for("update").limit(1))[0]; if (!invoice) throw new TRPCError({ code: "NOT_FOUND", message: "Invoice not found." }); const sale = (await tx.select().from(sales).where(eq(sales.id, invoice.saleId)).for("update").limit(1))[0]; if (!sale) throw new TRPCError({ code: "NOT_FOUND", message: "Sale for this invoice was not found." }); const total = Number(sale.total || 0); const previousPaid = Number(sale.paidAmount || 0); const amount = Number(input.amount.toFixed(3)); const remainingBefore = Math.max(0, total - previousPaid); if (remainingBefore <= 0.001) throw new TRPCError({ code: "BAD_REQUEST", message: "This invoice is already fully paid." }); if (amount > remainingBefore + 0.001) throw new TRPCError({ code: "BAD_REQUEST", message: `Payment cannot exceed the remaining balance of ${remainingBefore.toFixed(3)} BHD.` }); const paidTotal = Math.min(total, previousPaid + amount); const remainingAmount = Math.max(0, total - paidTotal); const nextStatus = paidTotal >= total - 0.001 ? "paid" : paidTotal > 0 ? "partial" : "unpaid"; const paymentResult = await tx.insert(invoicePayments).values({ invoiceId: invoice.id, amount: amount.toFixed(3), paymentMethod: input.paymentMethod, reference: input.reference || null, previousPaidAmount: previousPaid.toFixed(3), paidTotal: paidTotal.toFixed(3), remainingAmount: remainingAmount.toFixed(3), createdBy: ctx.user.id }).returning({ id: invoicePayments.id, createdAt: invoicePayments.createdAt }); await tx.insert(posPayments).values({ saleId: sale.id, method: input.paymentMethod, amount: amount.toFixed(3), reference: input.reference || `${invoice.invoiceNumber} payment`, createdBy: ctx.user.id }); await tx.update(sales).set({ paidAmount: paidTotal.toFixed(3), paymentStatus: nextStatus, paymentMethod: input.paymentMethod }).where(eq(sales.id, sale.id)); await tx.update(invoices).set({ status: nextStatus }).where(eq(invoices.id, invoice.id)); return { invoice, sale, payment: { id: Number(paymentResult[0]?.id || 0), invoiceId: invoice.id, invoiceNumber: invoice.invoiceNumber, amount: amount.toFixed(3), previousPaidAmount: previousPaid.toFixed(3), paidTotal: paidTotal.toFixed(3), remainingAmount: remainingAmount.toFixed(3), paymentMethod: input.paymentMethod, reference: input.reference || null, createdAt: paymentResult[0]?.createdAt || new Date() }, total, paidTotal, remainingAmount, paymentStatus: nextStatus }; }); await audit(ctx.user.id, "INVOICE_PAYMENT_ADDED", "invoice", input.invoiceId, { amount: result.payment.amount, paidTotal: result.paidTotal, remainingAmount: result.remainingAmount, paymentMethod: input.paymentMethod }); return result; }), delete: protectedProcedure.input(invoiceDeleteInput).mutation(async ({ ctx, input }) => { await access(ctx.user.id, ctx.user.role, adminRoles); const db = await dbOrThrow(); const result = await db.transaction(async tx => { const invoice = (await tx.select().from(invoices).where(eq(invoices.id, input.invoiceId)).for("update").limit(1))[0]; if (!invoice) throw new TRPCError({ code: "NOT_FOUND", message: "Invoice not found." }); const sale = (await tx.select().from(sales).where(eq(sales.id, invoice.saleId)).for("update").limit(1))[0]; if (!sale) throw new TRPCError({ code: "NOT_FOUND", message: "Sale for this invoice was not found." }); const lines = await tx.select().from(saleItems).where(eq(saleItems.saleId, sale.id)); const stockLines = lines.filter(line => line.inventoryItemId && Number(line.quantity) > 0); for (const line of stockLines) { const item = (await tx.select().from(inventoryItems).where(eq(inventoryItems.id, line.inventoryItemId!)).for("update").limit(1))[0]; if (!item) continue; const quantityBefore = Number(item.quantity || 0); const quantityAfter = quantityBefore + Number(line.quantity); await tx.update(inventoryItems).set({ quantity: three(quantityAfter) }).where(eq(inventoryItems.id, item.id)); await tx.insert(stockMovements).values({ inventoryItemId: item.id, movementType: "return", quantityChange: three(Number(line.quantity)), quantityBefore: three(quantityBefore), quantityAfter: three(quantityAfter), referenceType: "invoice_delete", referenceId: invoice.id, createdBy: ctx.user.id, notes: `Restored stock after deleting ${invoice.invoiceNumber}` }); } await tx.update(tailoringOrders).set({ saleId: null, updatedAt: new Date() }).where(eq(tailoringOrders.saleId, sale.id)); await tx.delete(invoiceDeliveries).where(eq(invoiceDeliveries.invoiceId, invoice.id)); await tx.delete(invoicePayments).where(eq(invoicePayments.invoiceId, invoice.id)); await tx.delete(posPayments).where(eq(posPayments.saleId, sale.id)); await tx.delete(saleItems).where(eq(saleItems.saleId, sale.id)); await tx.delete(invoices).where(eq(invoices.id, invoice.id)); await tx.delete(sales).where(eq(sales.id, sale.id)); return { invoiceNumber: invoice.invoiceNumber, saleNumber: sale.saleNumber, restoredStockLines: stockLines.length }; }); await audit(ctx.user.id, "INVOICE_DELETED", "invoice", input.invoiceId, result); return result; }), prepareDelivery: protectedProcedure.input(z.object({ invoiceId: z.number().int().positive(), channel: z.enum(["email", "whatsapp", "share"]), recipient: z.string().trim().max(320).optional(), message: z.string().trim().max(2000).optional() })).mutation(async ({ ctx, input }) => { await access(ctx.user.id, ctx.user.role, salesRoles); const db = await dbOrThrow(); const invoice = (await db.select().from(invoices).where(eq(invoices.id, input.invoiceId)).limit(1))[0]; if (!invoice) throw new TRPCError({ code: "NOT_FOUND", message: "Invoice not found." }); const result = await db.insert(invoiceDeliveries).values({ invoiceId: input.invoiceId, channel: input.channel, recipient: input.recipient || null, status: "prepared", message: input.message || null, createdBy: ctx.user.id }).returning({ id: invoiceDeliveries.id }); const deliveryId = id(result); await audit(ctx.user.id, "INVOICE_DELIVERY_PREPARED", "invoice", input.invoiceId, { channel: input.channel, recipient: input.recipient }); return { id: deliveryId, status: "prepared" as const }; }) }),
  staff: router({
  documents: router({
    list: protectedProcedure.input(z.object({ staffProfileId: z.number().int().positive() })).query(async ({ ctx, input }) => { await access(ctx.user.id, ctx.user.role, payrollRoles); return (await dbOrThrow()).select().from(staffDocuments).where(eq(staffDocuments.staffProfileId, input.staffProfileId)).orderBy(desc(staffDocuments.uploadedAt)).limit(100); }),
    download: protectedProcedure.input(z.object({ documentId: z.number().int().positive() })).mutation(async ({ ctx, input }) => { await access(ctx.user.id, ctx.user.role, payrollRoles); const document = (await (await dbOrThrow()).select().from(staffDocuments).where(eq(staffDocuments.id, input.documentId)).limit(1))[0]; if (!document) throw new TRPCError({ code: "NOT_FOUND", message: "Staff document not found." }); return { url: await storageGetSignedUrl(document.storageKey), fileName: document.fileName, contentType: document.contentType }; }),
    upload: protectedProcedure.input(z.object({ staffProfileId: z.number().int().positive(), label: z.string().trim().min(2).max(120), fileName: z.string().trim().min(1).max(255), contentType: z.string().trim().min(1).max(120), dataBase64: z.string().min(1).max(5000000) }).superRefine((value, ctx) => { const extension = value.fileName.toLowerCase().slice(value.fileName.lastIndexOf(".")); if (!staffDocumentMimeTypes.has(value.contentType) || !staffDocumentExtensions.has(extension)) ctx.addIssue({ code: "custom", path: ["fileName"], message: "Upload a PDF, PNG, JPG, WEBP, DOC, or DOCX staff document." }); })).mutation(async ({ ctx, input }) => { await access(ctx.user.id, ctx.user.role, payrollRoles); const profile = (await (await dbOrThrow()).select({ id: staffProfiles.id }).from(staffProfiles).where(eq(staffProfiles.id, input.staffProfileId)).limit(1))[0]; if (!profile) throw new TRPCError({ code: "NOT_FOUND", message: "Staff profile not found." }); const safeName = input.fileName.replace(/[^a-zA-Z0-9._-]/g, "_"); const stored = await storagePut(`staff/${input.staffProfileId}/${Date.now()}-${safeName}`, Buffer.from(input.dataBase64, "base64"), input.contentType); const result = await (await dbOrThrow()).insert(staffDocuments).values({ staffProfileId: input.staffProfileId, label: input.label, fileName: input.fileName, contentType: input.contentType, storageKey: stored.key, storageUrl: stored.url, uploadedBy: ctx.user.id }).returning({ id: staffDocuments.id }); const documentId = id(result); await audit(ctx.user.id, "STAFF_DOCUMENT_UPLOADED", "staffDocument", documentId, { staffProfileId: input.staffProfileId, label: input.label }); return { id: documentId, url: stored.url }; }),
  }),  list: protectedProcedure.query(async ({ ctx }) => { await access(ctx.user.id, ctx.user.role, staffDirectoryRoles); return (await dbOrThrow()).select().from(staffProfiles).where(eq(staffProfiles.isActive, true)).orderBy(staffProfiles.name); }), linkAccess: protectedProcedure.input(z.object({ staffProfileId: z.number().int().positive(), userId: z.number().int().positive() })).mutation(async ({ ctx, input }) => { await access(ctx.user.id, ctx.user.role, adminRoles); const db = await dbOrThrow(); const profile = (await db.select().from(staffProfiles).where(eq(staffProfiles.id, input.staffProfileId)).limit(1))[0]; if (!profile) throw new TRPCError({ code: "NOT_FOUND", message: "Staff profile not found." }); const user = (await db.select({ id: users.id }).from(users).where(eq(users.id, input.userId)).limit(1))[0]; if (!user) throw new TRPCError({ code: "NOT_FOUND", message: "ERP access account not found." }); const existing = (await db.select({ id: staffProfiles.id }).from(staffProfiles).where(eq(staffProfiles.userId, input.userId)).limit(1))[0]; if (existing && existing.id !== input.staffProfileId) throw new TRPCError({ code: "CONFLICT", message: "This ERP account is already linked to another payroll profile." }); await db.update(staffProfiles).set({ userId: input.userId, updatedAt: new Date() }).where(eq(staffProfiles.id, input.staffProfileId)); await audit(ctx.user.id, "STAFF_ACCESS_LINKED", "staffProfile", input.staffProfileId, { userId: input.userId }); return { success: true }; }), unlinkAccess: protectedProcedure.input(z.object({ staffProfileId: z.number().int().positive() })).mutation(async ({ ctx, input }) => { await access(ctx.user.id, ctx.user.role, adminRoles); const db = await dbOrThrow(); await db.update(staffProfiles).set({ userId: null, updatedAt: new Date() }).where(eq(staffProfiles.id, input.staffProfileId)); await audit(ctx.user.id, "STAFF_ACCESS_UNLINKED", "staffProfile", input.staffProfileId); return { success: true }; }), create: protectedProcedure.input(z.object({ name: z.string().min(2), phone: z.string(), jobTitle: z.string().min(2), baseSalary: z.number().min(0), commissionRate: z.number().min(0) })).mutation(async ({ ctx, input }) => { await access(ctx.user.id, ctx.user.role, payrollRoles); const result = await (await dbOrThrow()).insert(staffProfiles).values({ ...input, phone: input.phone || null, baseSalary: three(input.baseSalary), commissionRate: three(input.commissionRate) }).returning({ id: staffProfiles.id }); const staffId = id(result); await audit(ctx.user.id, "STAFF_CREATED", "staffProfile", staffId); return { id: staffId }; }), remove: protectedProcedure.input(z.object({ id: z.number().int().positive() })).mutation(async ({ ctx, input }) => { await access(ctx.user.id, ctx.user.role, payrollRoles); const db = await dbOrThrow(); const profile = (await db.select({ id: staffProfiles.id, name: staffProfiles.name }).from(staffProfiles).where(eq(staffProfiles.id, input.id)).limit(1))[0]; if (!profile) throw new TRPCError({ code: "NOT_FOUND", message: "Staff profile not found." }); await db.update(staffProfiles).set({ isActive: false, updatedAt: new Date() }).where(eq(staffProfiles.id, input.id)); await audit(ctx.user.id, "STAFF_ARCHIVED", "staffProfile", input.id, { name: profile.name }); return { success: true, id: input.id, name: profile.name }; }), recordAttendance: protectedProcedure.input(z.object({ staffProfileId: z.number().int(), workDate: z.string(), status: z.enum(["present", "absent", "leave", "half_day"]), absenceReason: z.enum(["sick", "unexcused", "personal", "other"]).optional(), absenceDetails: z.string().max(1000).optional(), absenceDocumentId: z.number().int().positive().optional(), notes: z.string() })).mutation(async ({ ctx, input }) => { await access(ctx.user.id, ctx.user.role, payrollRoles); const db = await dbOrThrow(); const profile = (await db.select().from(staffProfiles).where(eq(staffProfiles.id, input.staffProfileId)).limit(1))[0]; if (!profile) throw new TRPCError({ code: "NOT_FOUND", message: "Staff profile not found." }); const payPeriod = input.workDate.slice(0, 7); const absenceDeduction = absenceDeductionFor(Number(profile.baseSalary), input.status, payPeriod); await db.insert(attendance).values({ staffProfileId: input.staffProfileId, workDate: new Date(input.workDate), status: input.status, absenceReason: input.status === "present" ? null : input.absenceReason || null, absenceDetails: input.status === "present" ? null : input.absenceDetails?.trim() || null, absenceDocumentId: input.status === "present" ? null : input.absenceDocumentId || null, absenceDeduction: three(absenceDeduction), notes: input.notes || null, recordedBy: ctx.user.id }); await audit(ctx.user.id, "ATTENDANCE_RECORDED", "attendance", input.staffProfileId, { status: input.status, absenceDeduction }); return { success: true, absenceDeduction }; }), attendanceHistory: protectedProcedure.query(async ({ ctx }) => { await access(ctx.user.id, ctx.user.role, payrollRoles); return (await dbOrThrow()).select().from(attendance).orderBy(desc(attendance.workDate)).limit(100); }), performance: protectedProcedure.query(async ({ ctx }) => { await access(ctx.user.id, ctx.user.role, payrollRoles); return (await dbOrThrow()).select().from(performanceRecords).orderBy(desc(performanceRecords.workDate)).limit(100); }), addPerformance: protectedProcedure.input(z.object({ staffProfileId: z.number().int(), workDate: z.string(), metric: z.string().min(2), units: z.number().min(0), commissionEarned: z.number().min(0), notes: z.string() })).mutation(async ({ ctx, input }) => { await access(ctx.user.id, ctx.user.role, payrollRoles); await (await dbOrThrow()).insert(performanceRecords).values({ ...input, workDate: new Date(input.workDate), units: three(input.units), commissionEarned: three(input.commissionEarned), notes: input.notes || null, recordedBy: ctx.user.id }); await audit(ctx.user.id, "PERFORMANCE_RECORDED", "performanceRecord", input.staffProfileId); return { success: true }; }), payouts: protectedProcedure.query(async ({ ctx }) => { await access(ctx.user.id, ctx.user.role, payrollRoles); return (await dbOrThrow()).select().from(salaryPayouts).orderBy(desc(salaryPayouts.paidAt)).limit(100); }), removePayout: protectedProcedure.input(z.object({ id: z.number().int().positive() })).mutation(async ({ ctx, input }) => { await access(ctx.user.id, ctx.user.role, payrollRoles); const db = await dbOrThrow(); const payout = (await db.select({ id: salaryPayouts.id, payslipNumber: salaryPayouts.payslipNumber }).from(salaryPayouts).where(eq(salaryPayouts.id, input.id)).limit(1))[0]; if (!payout) throw new TRPCError({ code: "NOT_FOUND", message: "Payslip record not found." }); await db.delete(salaryPayouts).where(eq(salaryPayouts.id, input.id)); await audit(ctx.user.id, "SALARY_PAYOUT_DELETED", "salaryPayout", input.id, { payslipNumber: payout.payslipNumber }); return { success: true, payslipNumber: payout.payslipNumber }; }), calculate: protectedProcedure.input(z.object({ staffProfileId: z.number().int().positive(), payPeriod: z.string().regex(/^\d{4}-(0[1-9]|1[0-2])$/, "Choose a valid pay period."), allowances: z.number().min(0).max(1000000).default(0), overtime: z.number().min(0).max(1000000).default(0), deductions: z.number().min(0).max(1000000).default(0) })).query(async ({ ctx, input }) => { await access(ctx.user.id, ctx.user.role, payrollRoles); const db = await dbOrThrow(); const staff = (await db.select().from(staffProfiles).where(eq(staffProfiles.id, input.staffProfileId)).limit(1))[0]; if (!staff) throw new TRPCError({ code: "NOT_FOUND", message: "Staff profile not found." }); const { start, end } = getMonthBounds(input.payPeriod); const rows = await db.select().from(performanceRecords).where(and(eq(performanceRecords.staffProfileId, staff.id), gte(performanceRecords.workDate, start), lte(performanceRecords.workDate, end))); const attendanceRows = await db.select().from(attendance).where(and(eq(attendance.staffProfileId, staff.id), gte(attendance.workDate, start), lte(attendance.workDate, end))); const performanceBonus = rows.reduce((sum, row) => sum + Number(row.commissionEarned), 0); const baseSalary = Number(staff.baseSalary); const absenceDeduction = attendanceRows.reduce((sum, row) => sum + (Number(row.absenceDeduction) || absenceDeductionFor(baseSalary, row.status, input.payPeriod)), 0); const totalDeductions = input.deductions + absenceDeduction; const grossSalary = baseSalary + input.allowances + input.overtime + performanceBonus; const netSalary = grossSalary - totalDeductions; return { staffProfileId: staff.id, staffName: staff.name, payPeriod: input.payPeriod, baseSalary, allowances: input.allowances, overtime: input.overtime, performanceBonus, manualDeductions: input.deductions, absenceDeduction, deductions: totalDeductions, absenceCount: attendanceRows.filter(row => row.status === "absent").length, halfDayCount: attendanceRows.filter(row => row.status === "half_day").length, grossSalary, netSalary }; }), createPayout: protectedProcedure.input(z.object({ staffProfileId: z.number().int().positive(), payPeriod: z.string().regex(/^\d{4}-(0[1-9]|1[0-2])$/, "Choose a valid pay period."), amount: z.number().positive().max(1000000).optional(), allowances: z.number().min(0).max(1000000).default(0), overtime: z.number().min(0).max(1000000).default(0), performanceBonus: z.number().min(0).max(1000000).optional(), deductions: z.number().min(0).max(1000000).default(0), deductionDetails: z.string().max(2000).optional(), notes: z.string().max(2000) })).mutation(async ({ ctx, input }) => { await access(ctx.user.id, ctx.user.role, payrollRoles); const db = await dbOrThrow(); const staff = (await db.select().from(staffProfiles).where(eq(staffProfiles.id, input.staffProfileId)).limit(1))[0]; if (!staff) throw new TRPCError({ code: "NOT_FOUND", message: "Staff profile not found." }); const { start, end } = getMonthBounds(input.payPeriod); const rows = await db.select().from(performanceRecords).where(and(eq(performanceRecords.staffProfileId, staff.id), gte(performanceRecords.workDate, start), lte(performanceRecords.workDate, end))); const attendanceRows = await db.select().from(attendance).where(and(eq(attendance.staffProfileId, staff.id), gte(attendance.workDate, start), lte(attendance.workDate, end))); const performanceBonus = input.performanceBonus ?? rows.reduce((sum, row) => sum + Number(row.commissionEarned), 0); const baseSalary = Number(staff.baseSalary); const absenceDeduction = attendanceRows.reduce((sum, row) => sum + (Number(row.absenceDeduction) || absenceDeductionFor(baseSalary, row.status, input.payPeriod)), 0); const totalDeductions = input.deductions + absenceDeduction; const grossSalary = baseSalary + input.allowances + input.overtime + performanceBonus; const finalAmount = input.amount ?? grossSalary - totalDeductions; if (finalAmount <= 0) throw new TRPCError({ code: "BAD_REQUEST", message: "The calculated net salary must be greater than zero." }); const payslipNumber = `PS-${input.payPeriod.replace("-", "")}-${staff.id}-${Date.now()}`; const deductionDetails = [input.deductionDetails?.trim(), absenceDeduction > 0 ? `Automatic absence deduction: ${three(absenceDeduction)}` : ""].filter(Boolean).join(" · ") || null; const result = await db.insert(salaryPayouts).values({ staffProfileId: staff.id, payPeriod: input.payPeriod, payslipNumber, baseSalary: staff.baseSalary, allowances: three(input.allowances), overtime: three(input.overtime), performanceBonus: three(performanceBonus), deductions: three(totalDeductions), deductionDetails, netSalary: three(finalAmount), notes: input.notes.trim() || null, approvedBy: ctx.user.id }).returning({ id: salaryPayouts.id }); const payoutId = id(result); await audit(ctx.user.id, "SALARY_PAYOUT_CREATED", "salaryPayout", payoutId, { staffProfileId: staff.id, payPeriod: input.payPeriod, finalAmount: three(finalAmount), deductions: three(totalDeductions), absenceDeduction: three(absenceDeduction), payslipNumber }); return { id: payoutId, netSalary: finalAmount, payslipNumber, absenceDeduction, deductions: totalDeductions }; }) }),
  tailoring: router({ list: protectedProcedure.input(tailoringListInput.optional()).query(async ({ ctx, input }) => { await access(ctx.user.id, ctx.user.role, tailoringRoles); const db = await dbOrThrow(); const [orders, clientRows, tailorRows, measurementRows, saleRows, invoiceRows] = await Promise.all([db.select().from(tailoringOrders).orderBy(desc(tailoringOrders.createdAt)).limit(100), db.select().from(customers), db.select().from(staffProfiles), db.select().from(measurementProfiles), db.select({ id: sales.id, saleNumber: sales.saleNumber, paidAmount: sales.paidAmount, total: sales.total, paymentStatus: sales.paymentStatus, paymentMethod: sales.paymentMethod }).from(sales).orderBy(desc(sales.createdAt)).limit(500), db.select({ saleId: invoices.saleId, invoiceNumber: invoices.invoiceNumber, status: invoices.status }).from(invoices).limit(500)]); const clients = new Map(clientRows.map(client => [client.id, client])); const tailors = new Map(tailorRows.map(tailor => [tailor.id, tailor])); const measurements = new Map(measurementRows.map(profile => [profile.id, profile])); const salesById = new Map(saleRows.map(sale => [sale.id, sale])); const invoicesBySaleId = new Map(invoiceRows.map(invoice => [invoice.saleId, invoice])); const mappedOrders = orders.map(order => ({ ...order, sale: order.saleId ? salesById.get(order.saleId) || null : null, invoice: order.saleId ? invoicesBySaleId.get(order.saleId) || null : null, customerName: clients.get(order.customerId)?.name || "Archived customer", customerPhone: clients.get(order.customerId)?.phone || null, tailorName: order.assignedTailorId ? tailors.get(order.assignedTailorId)?.name || "Former tailor" : null, measurementVersion: order.measurementProfileId ? measurements.get(order.measurementProfileId)?.version || null : null })); const search = input?.search?.trim().toLowerCase(); return mappedOrders.filter(order => !search || [order.orderNumber, order.customerName, order.customerPhone || "", order.dueDate ? new Date(order.dueDate).toISOString().slice(0, 10) : "", order.createdAt ? new Date(order.createdAt).toISOString().slice(0, 10) : ""].some(value => String(value).toLowerCase().includes(search))); }), create: protectedProcedure.input(tailoringOrderInput).mutation(async ({ ctx, input }) => { await access(ctx.user.id, ctx.user.role, tailoringRoles); const db = await dbOrThrow(); const customer = (await db.select().from(customers).where(eq(customers.id, input.customerId)).limit(1))[0]; if (!customer) throw new TRPCError({ code: "NOT_FOUND", message: "Choose a valid customer for the tailoring order." }); const profile = input.measurementProfileId ? (await db.select().from(measurementProfiles).where(eq(measurementProfiles.id, input.measurementProfileId)).limit(1))[0] : null; const tailor = input.assignedTailorId ? (await db.select().from(staffProfiles).where(eq(staffProfiles.id, input.assignedTailorId)).limit(1))[0] : null; const relationError = tailoringRelationError(input.customerId, profile?.customerId, input.assignedTailorId ? Boolean(tailor?.isActive) : null); if (relationError === "measurement") throw new TRPCError({ code: "BAD_REQUEST", message: "Save or choose a measurement version belonging to this customer before confirming the tailoring order." }); if (relationError === "tailor") throw new TRPCError({ code: "BAD_REQUEST", message: "Choose an active tailor." }); const orderNumber = `TO-${Date.now()}`; const result = await db.insert(tailoringOrders).values({ ...input, orderNumber, measurementProfileId: input.measurementProfileId || null, assignedTailorId: input.assignedTailorId || null, dueDate: input.dueDate ? new Date(input.dueDate) : null, price: three(input.price), customerSuppliedFabric: input.customerSuppliedFabric, fabricNotes: input.fabricNotes || null, notes: input.notes || null, productionNotes: input.productionNotes || null, createdBy: ctx.user.id, status: "confirmed" }).returning({ id: tailoringOrders.id }); const orderId = id(result); await audit(ctx.user.id, "TAILORING_ORDER_CREATED", "tailoringOrder", orderId, { orderNumber, customerId: input.customerId, garmentType: input.garmentType, dueDate: input.dueDate }); return { id: orderId, orderNumber }; }), update: protectedProcedure.input(z.object({ id: z.number().int().positive(), assignedTailorId: z.number().int().positive().optional(), status: tailoringStatus, dueDate: z.string().optional(), productionNotes: z.string().max(3000) })).mutation(async ({ ctx, input }) => { await access(ctx.user.id, ctx.user.role, tailoringRoles); const db = await dbOrThrow(); const current = (await db.select().from(tailoringOrders).where(eq(tailoringOrders.id, input.id)).limit(1))[0]; if (!current) throw new TRPCError({ code: "NOT_FOUND", message: "Tailoring order not found." }); if (input.assignedTailorId) { const tailor = (await db.select().from(staffProfiles).where(eq(staffProfiles.id, input.assignedTailorId)).limit(1))[0]; if (!tailor?.isActive) throw new TRPCError({ code: "BAD_REQUEST", message: "Choose an active tailor." }); } await db.update(tailoringOrders).set({ assignedTailorId: input.assignedTailorId || null, status: input.status, dueDate: input.dueDate ? new Date(input.dueDate) : null, productionNotes: input.productionNotes || null }).where(eq(tailoringOrders.id, input.id)); await audit(ctx.user.id, "TAILORING_ORDER_UPDATED", "tailoringOrder", input.id, input); return { success: true }; }) }),
  team: router({ listRoles: protectedProcedure.query(async ({ ctx }) => { await access(ctx.user.id, ctx.user.role, adminRoles); const db = await dbOrThrow(); const baseRoles = await db.select({ userId: userBusinessRoles.userId, businessRole: userBusinessRoles.role, isActive: userBusinessRoles.isActive, name: users.name, email: users.email }).from(userBusinessRoles).leftJoin(users, eq(userBusinessRoles.userId, users.id)); const assignments = await db.select().from(userCustomRoles); const definitions = await db.select().from(customRoles); const byUser = new Map(assignments.map(assignment => [assignment.userId, assignment])); const byId = new Map(definitions.map(role => [role.id, role])); return baseRoles.map(base => { const assignment = byUser.get(base.userId); const customRole = assignment ? byId.get(assignment.customRoleId) : undefined; return { ...base, customRoleId: customRole?.id || null, customRoleName: customRole?.name || null, customRoleActive: Boolean(assignment?.isActive && customRole?.isActive) }; }); }), removeUser: protectedProcedure.input(z.object({ userId: z.number().int().positive() })).mutation(async ({ ctx, input }) => { await access(ctx.user.id, ctx.user.role, adminRoles); if (input.userId === ctx.user.id) throw new TRPCError({ code: "FORBIDDEN", message: "The owner account cannot be removed." }); const db = await dbOrThrow(); const target = (await db.select({ id: users.id, role: users.role }).from(users).where(eq(users.id, input.userId)).limit(1))[0]; if (!target) throw new TRPCError({ code: "NOT_FOUND", message: "This user no longer exists." }); const targetBusinessRole = (await db.select().from(userBusinessRoles).where(eq(userBusinessRoles.userId, input.userId)).limit(1))[0]; if (target.role === "admin" || targetBusinessRole?.role === "admin") throw new TRPCError({ code: "FORBIDDEN", message: "Owner accounts cannot be removed." }); await db.transaction(async tx => { await tx.delete(userCustomRoles).where(eq(userCustomRoles.userId, input.userId)); await tx.delete(userBusinessRoles).where(eq(userBusinessRoles.userId, input.userId)); await tx.delete(pendingAccessRequests).where(eq(pendingAccessRequests.userId, input.userId)); await tx.delete(staffAccessInvites).where(eq(staffAccessInvites.acceptedByUserId, input.userId)); await tx.update(staffProfiles).set({ userId: null, isActive: false, updatedAt: new Date() }).where(eq(staffProfiles.userId, input.userId)); await tx.delete(users).where(eq(users.id, input.userId)); }); await audit(ctx.user.id, "USER_REMOVED", "user", input.userId, { access: "revoked", records: "preserved" }); return { success: true }; }), listCustomRoles: protectedProcedure.query(async ({ ctx }) => { await access(ctx.user.id, ctx.user.role, adminRoles); const rows = await (await dbOrThrow()).select().from(customRoles).orderBy(customRoles.name); return rows.map(role => ({ ...role, permissions: Array.isArray(role.permissionsJson) ? role.permissionsJson.filter((value): value is string => typeof value === "string") : [] })); }), createCustomRole: protectedProcedure.input(z.object({ name: z.string().min(2).max(80), description: z.string().max(320), permissions: z.array(z.enum(customPermissionValues)).min(1) })).mutation(async ({ ctx, input }) => { await access(ctx.user.id, ctx.user.role, adminRoles); const result = await (await dbOrThrow()).insert(customRoles).values({ name: input.name.trim(), description: input.description.trim() || null, permissionsJson: input.permissions, createdBy: ctx.user.id }).returning({ id: customRoles.id }); const roleId = id(result); await audit(ctx.user.id, "CUSTOM_ROLE_CREATED", "customRole", roleId, input); return { id: roleId }; }), updateCustomRole: protectedProcedure.input(z.object({ id: z.number().int().positive(), name: z.string().min(2).max(80), description: z.string().max(320), permissions: z.array(z.enum(customPermissionValues)).min(1), isActive: z.boolean() })).mutation(async ({ ctx, input }) => { await access(ctx.user.id, ctx.user.role, adminRoles); await (await dbOrThrow()).update(customRoles).set({ name: input.name.trim(), description: input.description.trim() || null, permissionsJson: input.permissions, isActive: input.isActive }).where(eq(customRoles.id, input.id)); await audit(ctx.user.id, "CUSTOM_ROLE_UPDATED", "customRole", input.id, input); return { success: true }; }), assignCustomRole: protectedProcedure.input(z.object({ userId: z.number().int().positive(), customRoleId: z.number().int().positive(), isActive: z.boolean() })).mutation(async ({ ctx, input }) => { await access(ctx.user.id, ctx.user.role, adminRoles); const db = await dbOrThrow(); const role = (await db.select().from(customRoles).where(eq(customRoles.id, input.customRoleId)).limit(1))[0]; if (!role?.isActive) throw new TRPCError({ code: "BAD_REQUEST", message: "Choose an active owner-managed role." }); await db.insert(userCustomRoles).values({ ...input, updatedBy: ctx.user.id }).onConflictDoUpdate({ target: userCustomRoles.userId, set: { customRoleId: input.customRoleId, isActive: input.isActive, updatedBy: ctx.user.id } }); await audit(ctx.user.id, "CUSTOM_ROLE_ASSIGNED", "user", input.userId, input); return { success: true }; }), clearCustomRole: protectedProcedure.input(z.object({ userId: z.number().int().positive() })).mutation(async ({ ctx, input }) => { await access(ctx.user.id, ctx.user.role, adminRoles); await (await dbOrThrow()).delete(userCustomRoles).where(eq(userCustomRoles.userId, input.userId)); await audit(ctx.user.id, "CUSTOM_ROLE_CLEARED", "user", input.userId); return { success: true }; }), assignRole: protectedProcedure.input(z.object({ userId: z.number().int(), role: z.enum(["admin", "sales", "tailor", "inventory", "payroll"]), isActive: z.boolean() })).mutation(async ({ ctx, input }) => { await access(ctx.user.id, ctx.user.role, adminRoles); const db = await dbOrThrow(); await db.insert(userBusinessRoles).values(input).onConflictDoUpdate({ target: userBusinessRoles.userId, set: { role: input.role, isActive: input.isActive } }); await audit(ctx.user.id, "BUSINESS_ROLE_ASSIGNED", "user", input.userId, input); return { success: true }; }) }),
  access: router({ listInvites: protectedProcedure.query(async ({ ctx }) => { await access(ctx.user.id, ctx.user.role, adminRoles); const db = await dbOrThrow(); const [invites, roles] = await Promise.all([db.select().from(staffAccessInvites).orderBy(desc(staffAccessInvites.invitedAt)), db.select().from(customRoles)]); const roleById = new Map(roles.map(role => [role.id, role])); return invites.map(invite => ({ ...invite, roleName: roleById.get(invite.customRoleId)?.name || "Archived role" })); }), inviteStaff: protectedProcedure.input(z.object({ name: z.string().trim().min(2).max(160), email: z.string().trim().email().max(320), customRoleId: z.number().int().positive() })).mutation(async ({ ctx, input }) => { await access(ctx.user.id, ctx.user.role, adminRoles); const db = await dbOrThrow(); const role = (await db.select().from(customRoles).where(eq(customRoles.id, input.customRoleId)).limit(1))[0]; if (!role?.isActive) throw new TRPCError({ code: "BAD_REQUEST", message: "Choose an active role before preparing staff access." }); const email = input.email.toLowerCase(); await db.insert(staffAccessInvites).values({ name: input.name, email, customRoleId: role.id, invitedBy: ctx.user.id, isActive: true }).onConflictDoUpdate({ target: staffAccessInvites.email, set: { name: input.name, customRoleId: role.id, invitedBy: ctx.user.id, invitedAt: new Date(), isActive: true, acceptedByUserId: null, acceptedAt: null } }); await audit(ctx.user.id, "STAFF_ACCESS_PREPARED", "staffAccessInvite", undefined, { email, roleId: role.id }); return { email, roleName: role.name }; }), cancelInvite: protectedProcedure.input(z.object({ id: z.number().int().positive() })).mutation(async ({ ctx, input }) => { await access(ctx.user.id, ctx.user.role, adminRoles); const db = await dbOrThrow(); await db.update(staffAccessInvites).set({ isActive: false }).where(eq(staffAccessInvites.id, input.id)); await audit(ctx.user.id, "STAFF_ACCESS_CANCELLED", "staffAccessInvite", input.id); return { success: true } }), listPending: protectedProcedure.query(async ({ ctx }) => { await access(ctx.user.id, ctx.user.role, adminRoles); const db = await dbOrThrow(); const [requests, people] = await Promise.all([db.select().from(pendingAccessRequests).where(eq(pendingAccessRequests.status, "pending")).orderBy(desc(pendingAccessRequests.requestedAt)), db.select().from(users)]); const byId = new Map(people.map(person => [person.id, person])); return requests.map(request => ({ ...request, name: byId.get(request.userId)?.name || `User #${request.userId}`, email: byId.get(request.userId)?.email || null })); }), approvePending: protectedProcedure.input(z.object({ requestId: z.number().int().positive(), customRoleId: z.number().int().positive(), note: z.string().max(500) })).mutation(async ({ ctx, input }) => { await access(ctx.user.id, ctx.user.role, adminRoles); const db = await dbOrThrow(); const [request, role] = await Promise.all([db.select().from(pendingAccessRequests).where(eq(pendingAccessRequests.id, input.requestId)).limit(1).then(rows => rows[0]), db.select().from(customRoles).where(eq(customRoles.id, input.customRoleId)).limit(1).then(rows => rows[0])]); if (!request || request.status !== "pending") throw new TRPCError({ code: "NOT_FOUND", message: "This access request is no longer waiting for review." }); if (!role?.isActive) throw new TRPCError({ code: "BAD_REQUEST", message: "Choose an active owner-managed role." }); await db.transaction(async tx => { await tx.insert(userBusinessRoles).values({ userId: request.userId, role: "sales", isActive: true }).onConflictDoUpdate({ target: userBusinessRoles.userId, set: { isActive: true } }); await tx.insert(userCustomRoles).values({ userId: request.userId, customRoleId: role.id, isActive: true, updatedBy: ctx.user.id }).onConflictDoUpdate({ target: userCustomRoles.userId, set: { customRoleId: role.id, isActive: true, updatedBy: ctx.user.id } }); await tx.update(pendingAccessRequests).set({ status: "approved", reviewedAt: new Date(), reviewedBy: ctx.user.id, note: input.note || null }).where(eq(pendingAccessRequests.id, request.id)); }); await audit(ctx.user.id, "ACCESS_REQUEST_APPROVED", "pendingAccessRequest", request.id, { userId: request.userId, roleId: role.id }); return { success: true }; }), rejectPending: protectedProcedure.input(z.object({ requestId: z.number().int().positive(), note: z.string().max(500) })).mutation(async ({ ctx, input }) => { await access(ctx.user.id, ctx.user.role, adminRoles); const db = await dbOrThrow(); const request = (await db.select().from(pendingAccessRequests).where(eq(pendingAccessRequests.id, input.requestId)).limit(1))[0]; if (!request || request.status !== "pending") throw new TRPCError({ code: "NOT_FOUND", message: "This access request is no longer waiting for review." }); await db.update(pendingAccessRequests).set({ status: "rejected", reviewedAt: new Date(), reviewedBy: ctx.user.id, note: input.note || null }).where(eq(pendingAccessRequests.id, request.id)); await audit(ctx.user.id, "ACCESS_REQUEST_REJECTED", "pendingAccessRequest", request.id, { userId: request.userId }); return { success: true }; }) }),
  accessApproval: router({ approveWithPermissions: protectedProcedure.input(z.object({ requestId: z.number().int().positive(), name: z.string().trim().min(2).max(80), description: z.string().trim().max(320), permissions: z.array(z.enum(["dashboard", "customers", "sales", "inventory", "production", "payroll"])) .min(1), note: z.string().max(500) })).mutation(async ({ ctx, input }) => { await access(ctx.user.id, ctx.user.role, adminRoles); const db = await dbOrThrow(); const request = (await db.select().from(pendingAccessRequests).where(eq(pendingAccessRequests.id, input.requestId)).limit(1))[0]; if (!request || request.status !== "pending") throw new TRPCError({ code: "NOT_FOUND", message: "This access request is no longer waiting for review." }); const transaction = await db.transaction(async tx => { const roleResult = await tx.insert(customRoles).values({ name: input.name, description: input.description || null, permissionsJson: input.permissions, isActive: true, createdBy: ctx.user.id }).returning({ id: customRoles.id }); const roleId = id(roleResult); await tx.insert(userBusinessRoles).values({ userId: request.userId, role: "sales", isActive: true }).onConflictDoUpdate({ target: userBusinessRoles.userId, set: { isActive: true } }); await tx.insert(userCustomRoles).values({ userId: request.userId, customRoleId: roleId, isActive: true, updatedBy: ctx.user.id }).onConflictDoUpdate({ target: userCustomRoles.userId, set: { customRoleId: roleId, isActive: true, updatedBy: ctx.user.id } }); await tx.update(pendingAccessRequests).set({ status: "approved", reviewedAt: new Date(), reviewedBy: ctx.user.id, note: input.note || null }).where(eq(pendingAccessRequests.id, request.id)); return roleId; }); await audit(ctx.user.id, "ACCESS_REQUEST_APPROVED_WITH_PERMISSIONS", "pendingAccessRequest", request.id, { userId: request.userId, roleId: transaction }); return { roleId: transaction }; }) }),
  audit: protectedProcedure.query(async ({ ctx }) => { await access(ctx.user.id, ctx.user.role, adminRoles); return (await dbOrThrow()).select().from(auditLogs).orderBy(desc(auditLogs.createdAt)).limit(200); }),
});
